name: Design Assembly Expert
capability: design-assembler
description: Assemble all structured design artifacts into a cohesive final design document and requirements traceability matrix.
version: 0.1.1
skills:
  - design-assembler
scheduling:
  priority: 20
  dependencies: []  # Dynamically set to all active agents
policies:
  asset_baseline_required: true
  evidence_required: true
  output_must_be_structured: true
  descriptions_prefer_chinese: true
error_handling:
  on_missing_required_input: fail
  on_validation_failure: fail
tools:
  allowed:
    - write_file
    - patch_file
outputs:
  expected:
    - detailed-design.md
    - traceability.json
    - review-checklist.md

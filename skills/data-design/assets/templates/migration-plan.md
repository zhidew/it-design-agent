# 数据迁移与回滚计划: {{project_name}}

## 1. 变更范围
- **目标版本**: {{version}}
- **变更类型**: (例如：新增表 / 新增字段 / 索引优化 / 破坏性变更)
- **数据量预估**: (例如：表A 1000万级，表B 10万级)

## 2. 升级策略 (Up)
### 2.1 结构变更 (DDL)
详细的 DDL 变更请参考 `schema.sql`。

### 2.2 数据迁移 (DML)
- **是否需要数据清洗/补偿**: (是/否)
- **迁移步骤**:
  1. (如需，填写具体步骤，例如：编写脚本刷历史数据状态)
- **停机要求**: (不停机/短暂停机/需要业务低峰期执行)

## 3. 回滚策略 (Down)
**必须提供在发布失败时的回退方案。**

### 3.1 结构回滚 (Rollback DDL)
```sql
-- 示例：如果升级是建表，回滚就是删表
-- DROP TABLE IF EXISTS example_table;

-- 示例：如果升级是加字段，回滚就是删字段
-- ALTER TABLE example_table DROP COLUMN new_column;
```

### 3.2 数据回滚 (Rollback DML)
- **业务受损评估**: (如果回滚，新产生的数据如何处理？)
- **恢复步骤**: 
  1. (例如：通过双写机制，废弃新表数据，切回老表读写)

## 4. 验证清单
- [ ] DDL 已经在测试环境/预发环境执行成功。
- [ ] 慢查询风险已评估（新加查询是否都有索引）。
- [ ] DBA 已审核通过。

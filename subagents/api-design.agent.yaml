name: API设计子代理
capability: api-design
description: 基于需求基线、现有接口资产和约束策略生成项目级 API 设计产物。具备根据受众（内部/外部/两者）自动判定并产出对应 API 设计的能力，并沉淀校验证据。
version: 0.1.2
skills:
  - api-design
tools:
  allowed:
    - write_file
    - patch_file
outputs:
  expected:
    - api-internal.yaml
    - errors-rfc9457.json
policies:
  asset_baseline_required: true
  evidence_required: true
  output_must_be_structured: true
  manual_override_forbidden: true
  descriptions_prefer_chinese: true
error_handling:
  on_missing_required_input: fail
  on_validation_failure: fail
  on_partial_generation: emit_evidence_and_fail

name: 测试可设计性子代理
capability: test-design
description: 提取测试输入条件，生成覆盖率映射矩阵。
version: 0.1.1
skills:
  - test-design
policies:
  asset_baseline_required: true
  evidence_required: true
  output_must_be_structured: true
  descriptions_prefer_chinese: true
error_handling:
  on_missing_required_input: fail
  on_validation_failure: fail
tools:
  allowed: [write_file, patch_file]
outputs:
  expected: [test-plan.md, test-cases.yaml]
